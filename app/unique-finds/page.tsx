import type { Metadata } from "next";

export const metadata: Metadata = {
  title: "Unique Finds — P&R",
  description: "Discover P&R Unique Finds: limited T-shirts made in quantities of five or fewer, never restocked or reproduced.",
  alternates: { canonical: "/unique-finds" },
};

export { default } from "../page";
